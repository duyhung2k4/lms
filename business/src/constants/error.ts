export enum ERROR_QUERY_DB {
  ITEM_NOT_FOUND = "item not found!",
}

export enum ERROR_BAD_REQUEST {
  QUERY_NOT_FOUND = "query not found!",
  TYPE_OF_QUERY_INVALID = "typeof query invalid!"
}

export enum ERROR_AUTH {
  TOKEN_NOT_FOUND = "token not found!",
}